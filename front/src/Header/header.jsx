import React from "react";
import s from './header.module.css';

const Header = (props) => {
    return (
        <div className={s.head}>
            Header
        </div>
    )
}
export default Header;